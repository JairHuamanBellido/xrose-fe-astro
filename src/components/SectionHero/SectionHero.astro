---
import type { SectionHeroDomain } from "../../lib/shared/components-model/sectionHero/domain/model/SectionHeroDomain.model";

interface Props extends SectionHeroDomain {}

const { heading, htmlBody } = Astro.props;
---

<section
  class="max-w-[1440px] mx-auto h-[700px] flex justify-between items-center"
>
  <div class="w-2/3">
    <h1 class="text-5xl leading-normal font-semibold mb-8">{heading}</h1>
    {htmlBody && <div class="text-gray-600 text-base" set:html={htmlBody} />}
  </div>
  <div class="w-1/3">
    <p>an IMAGE</p>
  </div>
</section>
